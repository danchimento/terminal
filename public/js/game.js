export default class Game {

    constructor(element) {
        var typed = new Typed(element, {
            strings: ["First sentence.", "Second sentence."],
            typeSpeed: 30
          });
    }

}